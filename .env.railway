# Environment Variables สำหรับ Railway Deployment
# คัดลอกและตั้งค่าใน Railway Dashboard → Variables

# ==================== REQUIRED (จำเป็น) ====================

# Binance API Credentials
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_api_secret_here

# Security Keys (Generate random strings)
JWT_SECRET_KEY=your_jwt_secret_key_minimum_32_characters_long
ENCRYPTION_KEY=your_encryption_key_32_characters_long

# ==================== OPTIONAL (แนะนำ) ====================

# Binance Network
BINANCE_BASE_URL=https://testnet.binance.vision
# Production: https://api.binance.com
# Testnet: https://testnet.binance.vision

# Telegram Notifications (optional)
TELEGRAM_ENABLED=false
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id

# ==================== AUTO-CONFIGURED (Railway ตั้งให้) ====================
# DATABASE_URL=postgresql://... (Railway จัดการให้อัตโนมัติ)
# PORT=8000 (Railway จัดการให้อัตโนมัติ)

# ==================== วิธีสร้าง Secret Keys ====================
# ใช้ PowerShell:
# -d $((-join (1..32 | ForEach-Object {[char]((65..90) + (97..122) + (48..57) | Get-Random)})))
# หรือ Python:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
