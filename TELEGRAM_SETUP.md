# 📱 Telegram Bot Setup Guide

## 🎯 ภาพรวม
คู่มือการตั้งค่า Telegram Bot เพื่อรับการแจ้งเตือนและควบคุม Trading Bot

---

## 📋 ขั้นตอนการตั้งค่า (5 นาที)

### ขั้นตอนที่ 1: สร้าง Telegram Bot

1. **เปิด Telegram** (มือถือหรือคอมพิวเตอร์)

2. **ค้นหา BotFather**
   - พิมพ์ `@BotFather` ในช่องค้นหา
   - เลือก BotFather (มีเครื่องหมายถูกสีน้ำเงิน ✓)

3. **สร้าง Bot ใหม่**
   ```
   ส่งคำสั่ง: /newbot
   ```

4. **ตั้งชื่อ Bot**
   ```
   BotFather จะถาม: "Alright, a new bot. How are we going to call it?"
   ตอบ: My Trading Bot
   (หรือชื่ออะไรก็ได้ที่ชอบ)
   ```

5. **ตั้ง Username**
   ```
   BotFather จะถาม: "Good. Now let's choose a username for your bot."
   ตอบ: my_trading_bot
   (ต้องลงท้ายด้วย 'bot' และไม่มีใครใช้แล้ว)
   ```

6. **คัดลอก Bot Token** 🔑
   ```
   BotFather จะให้ Token แบบนี้:
   
   1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-aBcDeFg
   
   ⚠️ เก็บ Token นี้ไว้ให้ดี - อย่าแชร์ให้ใคร!
   ```

---

### ขั้นตอนที่ 2: หา Chat ID

1. **ค้นหา Bot ที่สร้าง**
   - พิมพ์ username ของ bot ที่สร้างไว้ (เช่น @my_trading_bot)
   - คลิกเข้าไป

2. **ส่งข้อความ**
   ```
   ส่งข้อความ: /start
   ```
   (Bot อาจจะยังไม่ตอบ - ไม่เป็นไร)

3. **รัน Script หา Chat ID**
   
   **วิธีที่ 1: ใช้ Script (แนะนำ)**
   ```bash
   cd C:\Users\DEVLeader\Desktop\BinanceBot
   python utils/get_chat_id.py
   ```
   
   Script จะถาม Bot Token → ใส่ token ที่คัดลอกไว้
   
   Output จะเป็นแบบนี้:
   ```
   ✓ Found Chats:
   
   Chat ID: 123456789
   Name: Your Name
   Username: @your_username
   ========================================
   
   ✓ Copy your Chat ID and paste into .env
   ```

   **วิธีที่ 2: ใช้ Browser (ถ้า script ไม่ได้)**
   
   เปิด browser แล้วไปที่:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
   (แทน <YOUR_BOT_TOKEN> ด้วย token ที่ได้)
   
   หาค่า `"chat":{"id":123456789}` → เก็บตัวเลข Chat ID ไว้

---

### ขั้นตอนที่ 3: แก้ไขไฟล์ .env

1. **เปิดไฟล์ .env**
   ```
   C:\Users\DEVLeader\Desktop\BinanceBot\.env
   ```

2. **แก้ไขค่าเหล่านี้:**
   ```env
   TELEGRAM_ENABLED=true
   TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-aBcDeFg
   TELEGRAM_CHAT_ID=123456789
   ```
   
   แทนที่:
   - `1234567890:ABC...` → Bot Token ของคุณ
   - `123456789` → Chat ID ของคุณ

3. **บันทึกไฟล์** (Ctrl+S)

---

### ขั้นตอนที่ 4: ทดสอบการทำงาน

1. **รัน Test Script**
   ```bash
   python test_telegram.py
   ```

2. **ตรวจสอบผลลัพธ์**
   
   **ถ้าสำเร็จ:**
   ```
   ============================================================
                   📱 TELEGRAM BOT TEST
   ============================================================

   1️⃣ ตรวจสอบการตั้งค่า:
      TELEGRAM_ENABLED: True
      BOT_TOKEN: ✅ Set
      CHAT_ID: ✅ Set

   2️⃣ ทดสอบส่งข้อความ...
      ✅ ส่งข้อความสำเร็จ!

   3️⃣ ตรวจสอบใน Telegram:
      📱 เปิด Telegram และดูข้อความทดสอบ

   ✅ Telegram Bot พร้อมใช้งาน!

   💡 คำสั่งที่ใช้ได้:
      /start  - เริ่มบอท
      /status - ดูสถานะ
      /balance - ดูยอดเงิน
      /positions - ดูออเดอร์
      /help - ดูคำสั่งทั้งหมด
   ============================================================

   🎉 Setup สำเร็จ! พร้อมใช้งาน Bot
   ```

   **เช็คใน Telegram:**
   - ควรเห็นข้อความทดสอบจาก bot
   - ลองส่ง `/help` → bot ควรตอบกลับ

3. **ถ้ามี Error:**
   
   **Error: BOT_TOKEN ไม่ถูกต้อง**
   - ตรวจสอบ token อีกครั้ง
   - คัดลอกทั้งหมดรวม `:` ตรงกลาง
   - ไม่มีช่องว่างข้างหน้าหรือข้างหลัง

   **Error: CHAT_ID ไม่ถูกต้อง**
   - รัน `python utils/get_chat_id.py` ใหม่
   - ตรวจสอบว่าส่ง `/start` ให้ bot แล้ว
   - ใช้ตัวเลขเท่านั้น (ไม่มี @ หรืออย่างอื่น)

   **Error: Timeout**
   - ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
   - ลองรันใหม่อีกครั้ง

---

### ขั้นตอนที่ 5: เริ่มใช้งาน Bot

1. **รัน Trading Bot**
   ```bash
   python -m bots.daily_scalping_bot
   ```
   หรือ
   ```bash
   RUN_BOT.bat
   ```

2. **ทดสอบคำสั่งใน Telegram**
   
   ส่งคำสั่งต่อไปนี้ไปที่ bot:
   ```
   /start    → เริ่มบอท
   /status   → ดูสถานะปัจจุบัน
   /balance  → ดูยอดเงิน
   /help     → ดูคำสั่งทั้งหมด
   ```

3. **รอรับการแจ้งเตือน**
   
   Bot จะแจ้งเตือนอัตโนมัติเมื่อ:
   - 🟢 เปิดออเดอร์ใหม่
   - ✅ ปิดออเดอร์กำไร
   - ❌ ปิดออเดอร์ขาดทุน
   - 💰 Partial Take Profit
   - 🔴 Recovery Mode
   - 🎉 ถึงเป้าหมายรายวัน

---

## 📱 คำสั่งที่ใช้ได้

### 🎮 การควบคุม
| คำสั่ง | คำอธิบาย |
|--------|----------|
| `/start` | เริ่มบอท |
| `/stop` | หยุดบอท |
| `/pause` | หยุดเทรดชั่วคราว |
| `/resume` | เทรดต่อ |
| `/help` | ดูคำสั่งทั้งหมด |

### 💰 ข้อมูลการเทรด
| คำสั่ง | คำอธิบาย |
|--------|----------|
| `/status` | สถานะปัจจุบัน |
| `/balance` | ยอดเงิน |
| `/positions` | ออเดอร์ที่เปิดอยู่ |
| `/trades` | ประวัติเทรดล่าสุด |
| `/stats` | สถิติการเทรด |

### 📊 การวิเคราะห์
| คำสั่ง | คำอธิบาย |
|--------|----------|
| `/symbols` | Active symbols |
| `/price [SYMBOL]` | ราคาปัจจุบัน |
| `/logic [SYMBOL]` | วิเคราะห์สัญญาณ |
| `/settings` | การตั้งค่าบอท |

---

## 🔒 ความปลอดภัย

### ⚠️ ข้อควรระวัง
- 🔐 **อย่าแชร์ Bot Token** - ใครได้ token ไปจะส่งข้อความแทนบอทได้
- 🔐 **อย่า commit .env** - มี .gitignore ป้องกันอยู่แล้ว
- 🔐 **ตั้งรหัสผ่าน Telegram** - ป้องกันคนอื่นเข้าดู
- 🔐 **ปิด bot เมื่อไม่ใช้** - ส่ง `/stop` หรือปิดโปรแกรม

### ✅ Bot จะตอบกลับเฉพาะ
- Chat ID ที่ตั้งไว้ใน .env เท่านั้น
- คำสั่งอื่นๆ จะได้รับ "⚠️ Unauthorized"

---

## ❓ แก้ปัญหา (Troubleshooting)

### ❌ Bot ไม่ตอบใน Telegram

**ตรวจสอบ:**
1. `TELEGRAM_ENABLED=true` ใน .env?
2. Bot Token ถูกต้อง?
3. Chat ID ถูกต้อง?
4. Bot กำลังรันอยู่?

**วิธีแก้:**
```bash
# ทดสอบอีกครั้ง
python test_telegram.py

# ถ้ายังไม่ได้ ลองรันใหม่
python -m bots.daily_scalping_bot
```

---

### ❌ Error: "Unauthorized"

**สาเหตุ:** Chat ID ไม่ตรงกับที่ตั้งใน .env

**วิธีแก้:**
1. รัน `python utils/get_chat_id.py` ใหม่
2. ใส่ Bot Token
3. คัดลอก Chat ID ที่ถูกต้อง
4. แก้ไขใน .env
5. Restart bot

---

### ❌ Error: "Invalid token"

**สาเหตุ:** Bot Token ไม่ถูกต้อง

**วิธีแก้:**
1. ไป @BotFather ใน Telegram
2. ส่ง `/mybots`
3. เลือก bot ของคุณ
4. เลือก "API Token"
5. คัดลอก token ทั้งหมด
6. แก้ไขใน .env
7. Restart bot

---

### ⚠️ Bot ส่งข้อความช้า

**สาเหตุ:** Telegram API อาจล่าช้า (ปกติ 1-3 วินาที)

**ไม่ต้องกังวล:**
- การแจ้งเตือนจะมาครบทุกรายการ
- ไม่มีผลต่อการเทรด (เทรดเป็น real-time)

---

### 🐛 อื่นๆ

**Bot ส่งข้อความซ้ำ:**
- ไม่ควรรัน bot หลายตัวพร้อมกัน
- ปิด bot ตัวเก่าให้หมดก่อนเปิดใหม่

**ไม่เห็น keyboard/buttons:**
- Bot นี้ใช้คำสั่งแบบพิมพ์ (เช่น /status)
- ไม่มี inline keyboard

**ต้องการเปลี่ยน bot:**
- สร้าง bot ใหม่ที่ @BotFather
- คัดลอก token ใหม่
- แก้ไขใน .env
- Restart

---

## 🎉 เสร็จสิ้น!

ตอนนี้คุณมี Trading Bot ที่:
- ✅ รับการแจ้งเตือนแบบ Real-time
- ✅ ควบคุมได้ผ่าน Telegram
- ✅ ตรวจสอบสถานะได้ทุกที่ทุกเวลา

**Happy Trading! 🚀**

---

## 📚 เอกสารเพิ่มเติม

- [คำสั่ง Telegram ทั้งหมด](TELEGRAM_COMMANDS_V2.md)
- [คู่มือเริ่มต้น](docs/QUICK_START_V2.md)
- [ฟีเจอร์อัจฉริยะ](docs/INTELLIGENT_FEATURES.md)
