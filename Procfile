# Procfile for Railway deployment
# Defines which processes to run

# Web server (main dashboard)
web: python app.py

# Background worker (trading bot) - optional separate service
# worker: python -m bots.aggressive_recovery_bot
