# 📁 Logs Folder

โฟลเดอร์นี้เก็บบันทึกการทำงานของบอททั้งหมด (สร้างอัตโนมัติ)

## 📝 ไฟล์ Log ที่สร้างขึ้น

บอทจะสร้างไฟล์ log อัตโนมัติเมื่อรัน:

- **bot_YYYYMMDD.log** - บันทึกรายวัน
  - ตัวอย่าง: `bot_20260203.log`
  - เก็บทุกการกระทำของบอทในวันนั้น

- **error_YYYYMMDD.log** - บันทึก Error (ถ้ามี)
  - เก็บเฉพาะ Error และ Warning
  - ใช้สำหรับ Debug

---

## 📊 ข้อมูลใน Log File

### ตัวอย่างเนื้อหา:
```
[10:30:15] 🚀 Starting Daily Scalping Bot v3.0
[10:30:16] ✅ Connected to Binance API
[10:30:17] 📊 Active Symbols: BTCUSDT, ETHUSDT, BNBUSDT
[10:30:45] 📈 BUY Signal: BTCUSDT at $50,000
[10:31:20] ✅ Order Filled: BTCUSDT, Size: 0.002 BTC
[10:35:45] 💰 TP Hit: BTCUSDT, Profit: +$1.20 (1.2%)
[10:35:46] ✅ Position Closed: BTCUSDT
```

---

## 🔍 วิธีดู Log Files

### ด้วย Text Editor
```bash
# เปิดด้วย Notepad
notepad logs\bot_20260203.log

# หรือ VS Code
code logs\bot_20260203.log
```

### ดู Real-time (ขณะบอทรันอยู่)
```bash
# Windows PowerShell
Get-Content logs\bot_20260203.log -Wait -Tail 20

# หรือใช้ tail (ถ้ามี Git Bash)
tail -f logs/bot_20260203.log
```

---

## 🗑️ การลบ Log Files

Log files จะเพิ่มขนาดขึ้นเรื่อยๆ ควรลบเป็นระยะ:

### ลบ Log เก่า (เก็บไว้ 7 วันล่าสุด)
```powershell
# ลบ Log ที่เก่ากว่า 7 วัน
Get-ChildItem logs\*.log | Where-Object {$_.LastWriteTime -lt (Get-Date).AddDays(-7)} | Remove-Item
```

### ลบทั้งหมด
```bash
# ⚠️ ลบ Log ทั้งหมด (ระวัง!)
del logs\*.log
```

---

## 📈 การใช้ Log สำหรับการวิเคราะห์

### หา Error ทั้งหมด
```powershell
# ค้นหา ERROR ในล็อก
Select-String -Path logs\*.log -Pattern "ERROR" | Format-Table -AutoSize
```

### นับจำนวนการเทรด
```powershell
# นับจำนวน BUY และ SELL
Select-String -Path logs\bot_20260203.log -Pattern "Order Filled" | Measure-Object
```

### หาการเทรดที่กำไร
```powershell
# ค้นหาการเทรดที่กำไร
Select-String -Path logs\bot_20260203.log -Pattern "TP Hit" | Format-Table -AutoSize
```

---

## ⚙️ การตั้งค่า Logging

สามารถปรับระดับ logging ได้ใน `config/config.py`:

```python
# Logging Level
LOG_LEVEL = "INFO"  # ตัวเลือก: DEBUG, INFO, WARNING, ERROR

# เก็บ Log กี่วัน
LOG_RETENTION_DAYS = 7
```

---

## 🔒 ความปลอดภัย

- Log files ไม่มี API Keys (ปลอดภัย)
- แต่มีข้อมูลการเทรด (ราคา, ขนาด, กำไร)
- อย่าแชร์ log files ในที่สาธารณะ

---

## 💡 Tips

1. **ตรวจสอบ Log เป็นประจำ** - ดูว่าบอททำงานปกติหรือไม่
2. **เก็บ Log ย้อนหลัง** - สำหรับวิเคราะห์ผลงาน
3. **ลบ Log เก่า** - ประหยัดพื้นที่ HDD/SSD
4. **ใช้ Grep/Select-String** - ค้นหาข้อมูลง่ายขึ้น
