# 📁 Data Folder

โฟลเดอร์นี้เก็บข้อมูลสถานะและประวัติการทำงานของบอท

## 📝 ไฟล์ในโฟลเดอร์นี้

### 📊 Bot State Files

- **bot_state.json** - สถานะการทำงานของ Daily Scalping Bot
  - เก็บประวัติการเทรดทั้งหมด
  - ตำแหน่งที่เปิดอยู่
  - กำไร/ขาดทุนสะสม
  - สถิติการเทรด

- **bot_state_aggressive.json** - สถานะการทำงานของ Aggressive Recovery Bot
  - เก็บประวัติการเทรดทั้งหมด
  - ระดับ Martingale ปัจจุบัน
  - ตำแหน่งที่เปิดอยู่
  - กำไร/ขาดทุนสะสม
  - สถิติการกู้คืน

---

## ⚠️ คำเตือนสำคัญ

### ❌ อย่าลบไฟล์เหล่านี้!

ไฟล์เหล่านี้มีข้อมูลสำคัญ:
- ประวัติการเทรดทั้งหมด
- ตำแหน่งที่เปิดอยู่ (ถ้าลบจะสูญหายและบอทจะไม่รู้ว่ามี position เปิดอยู่)
- สถิติผลงาน
- ข้อมูลสำหรับการเรียนรู้ของ AI

### 🔄 การ Backup

แนะนำให้ backup ไฟล์เหล่านี้เป็นประจำ:
```bash
# สร้างโฟลเดอร์ backup
mkdir backup

# คัดลอกไฟล์ไปเก็บ
copy data\bot_state*.json backup\
```

### 🗑️ การลบข้อมูล

ถ้าต้องการเริ่มต้นใหม่ (ลบประวัติทั้งหมด):
1. **ปิดบอทก่อน!** (สำคัญมาก)
2. ลบ `bot_state.json` และ `bot_state_aggressive.json`
3. เปิดบอทใหม่ จะสร้างไฟล์ใหม่อัตโนมัติ

---

## 📊 โครงสร้างข้อมูลใน bot_state.json

```json
{
  "starting_balance": 100.0,
  "current_balance": 105.5,
  "total_profit": 5.5,
  "total_profit_percent": 5.5,
  "total_trades": 25,
  "winning_trades": 18,
  "losing_trades": 7,
  "win_rate": 72.0,
  "open_positions": [...],
  "trade_history": [...],
  "last_update": "2026-02-03T10:30:00"
}
```

---

## 🔍 วิธีดูข้อมูล

### ด้วย Text Editor
```bash
# เปิดด้วย Notepad
notepad data\bot_state.json

# หรือ VS Code
code data\bot_state.json
```

### ด้วย Python
```python
import json

# อ่านไฟล์
with open('data/bot_state.json', 'r') as f:
    state = json.load(f)

# แสดงผล
print(f"Total Profit: ${state['total_profit']:.2f}")
print(f"Win Rate: {state['win_rate']:.1f}%")
```

---

## 🔒 ความปลอดภัย

- ไฟล์เหล่านี้ไม่มีข้อมูลที่ sensitive (ไม่มี API Key)
- แต่มีประวัติการเทรดทั้งหมด ควรเก็บเป็นความลับ
- อย่าแชร์ไฟล์เหล่านี้ในที่สาธารณะ
